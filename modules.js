// const xyz = require("./people");
// const os = require("os");

// const { people, ages} = require("./people"); // object destructuring only occurs when the exporting object have the same property name which we are used in the destructuring process

// console.log(xyz); // it is an empty object
// console.log(people);
// console.log(xyz.people);

// console.log(os.platform(), os.homedir());

// console.log(people, ages);
